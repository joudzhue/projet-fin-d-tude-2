# À installer hors du dépôt, par exemple /etc/grod/backup.env (chmod 600).
BACKUP_DIR=/var/backups/grod
BACKUP_RETENTION_DAYS=14

# DATABASE_PASSWORD et les autres DATABASE_* proviennent normalement de
# /etc/grod/grod.env, également protégé en 600.
